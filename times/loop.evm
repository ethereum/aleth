.code:
  PUSH 60		contract loop {...
  PUSH 40		contract loop {...
  MSTORE			contract loop {...
  PUSH 0		int i
tag3: 
  JUMPDEST		for (int i = 0; i < 100000; +1) {...
  PUSH [tag3]		for (int i = 0; i < 100000; +1) {...
  JUMP			for (int i = 0; i < 100000; +1) {...
.data:
  0: 
  .code:
    PUSH 60		contract loop {...
    PUSH 40		contract loop {...
    MSTORE			contract loop {...
    STOP			contract loop {...
